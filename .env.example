# Keep the complete Discord webhook URL secret. Do not put it in YAML or commit it.
DISCORD_WEBHOOK_URL=

# Safe default: evaluate listings and log sanitized embed payloads without sending them.
DRY_RUN=true

# Used only when starting the service outside Docker.
CONFIG_PATH=config.example.yaml

